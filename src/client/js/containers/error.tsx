import * as React from 'react';

export default class Error extends React.Component<any, any> {
    render() {
        return (
            <div>
                Error
            </div>
        );
    }
}